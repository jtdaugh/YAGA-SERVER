language: python
python:
  - "2.7"
install:
  - make install
script:
  - make lint
  - make test
services:
  - redis-server
  - memcached
  - rabbitmq
addons:
  postgresql: "9.3"
before_install:
  - psql -c 'create database yaga_test;' -U postgres
